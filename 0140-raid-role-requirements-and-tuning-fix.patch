diff --git a/guild-idler-status.md b/guild-idler-status.md
index 4ff40fc..0e38ee3 100644
--- a/guild-idler-status.md
+++ b/guild-idler-status.md
@@ -9258,3 +9258,128 @@ resize fix above applies (can't drive an actual multi-display Electron
 session in this environment), so this is a code-path/logic-level
 verification, not a live on-screen one.
 
+### Raid role requirements: Heroic/Mythic success ceiling, per-raid data, and a Tuning tab data-integrity fix -- built (patch 0140)
+
+```discord-update
+Dev Update | Raid Role Requirements
+
+- Added role requirements to all 8 raids, matching each raid's theme
+- Bringing the wrong party still works, but odds drop further the more roles you're missing
+- On Heroic and Mythic, an unmet role requirement now caps how high your odds can go, no matter how strong the party is
+- Fixed the DevTool's Tuning tab, which couldn't save at all due to 28 pre-existing entries missing required data
+```
+
+Follow-up to "Melee/Ranged/Caster Hero Roles -- built (patch 0135)," which
+shipped the full role/training/mismatch-penalty infrastructure but
+deliberately left every raid's `requiredRoles` empty as a balance
+decision for later. This patch is that balance decision, plus a design
+change to how the mismatch behaves at higher difficulty, worked through
+directly with the requester rather than assumed.
+
+**The existing subtractive penalty is unchanged and still the base
+mechanism.** `RaidManager.roleMismatchPenalty` still sums `max(0, needed
+- have)` across every required role and multiplies by
+`Tuning.get('raid.roleMismatchPenaltyPerSlot')` (default 8) -- e.g. a
+raid wanting `{melee: 2, caster: 1}` against a party with 0 melee, 1
+caster is 2 unmet slots, a 16-point subtraction from whatever the
+party's gear/level would otherwise have computed. Confirmed this already
+scales correctly with however many slots are unmet and can only ever
+subtract, never add -- no change needed there.
+
+**New: a Heroic/Mythic-only success ceiling, layered on top of the
+existing penalty, not a replacement for it.** `RaidDifficultyConfig`
+gains an optional `roleMismatchCap` (`types.ts`) -- undefined on Normal,
+so a mismatched Normal party still only eats the ordinary subtraction and
+can climb back to `MAX_SUCCESS` (95) on gear/level alone, same as before
+this existed. Heroic and Mythic read theirs from two new tuning entries
+(`raid_difficulty.heroic.roleMismatchCap` = 65,
+`raid_difficulty.mythic.roleMismatchCap` = 45 -- Mythic deliberately
+lower, same relationship its `successPenalty` already has), wired into
+`RAID_DIFFICULTIES` in `raids.ts` the same way successPenalty/
+lootBonus/durationMultiplier already are. A new `RaidManager.
+hasRoleMismatch` boolean (kept deliberately separate from
+`roleMismatchPenalty`'s point value, so the cap still engages even if
+someone tunes the per-slot penalty down to 0) gates it: once a party is
+missing any required slot, Heroic/Mythic success can't rise above the
+cap no matter how far over gear/level would otherwise push it. Applied
+identically in `previewEncounterSuccess` (the UI's live odds) and in
+`resolve`'s actual per-encounter roll, so the number the player sees
+before committing is never better than what they'll actually get.
+
+**Confirmed and preserved: role mismatch is evaluated once per raid, not
+per encounter.** `active.partySuccessBonus` (which already has
+`roleMismatchPenalty` folded in) gets computed once at `start()` and
+reused for every encounter in `resolve()`'s loop -- the party can't
+change mid-raid, so there was never a reason to re-derive this per
+encounter. The new `hasRoleMismatch` cap check follows the same pattern:
+computed once before `resolve()`'s encounter loop, not inside it.
+
+**`requiredRoles` populated on all 8 raids** (`raids.json`) -- data-only,
+fully DevTool-editable going forward (the field type already existed as
+of patch 0135's schema work; no new DevTool code needed). Thematic,
+scaling with level, and deliberately kept small enough to be achievable
+at Normal's 3-hero party size for every raid except the capstone:
+
+- Blackford Keep (8): none -- first raid, teaches the raid system itself
+  before layering role complexity on top
+- Frozen Wyrmkeep (18): ranged 1 -- anti-air against a dragon brood
+- Bonewrought Vault (22): caster 1 -- an undead horde
+- What Got Out (26): melee 1, caster 1
+- Black Dragon Nest (30): melee 1, ranged 1
+- House of Bones (41): caster 2 -- a lich's ritual
+- Silence the Loom (43): melee 1, ranged 1, caster 1 -- one straight boss
+  fight, no lesser threats to clear first, wants a genuinely balanced
+  party
+- Requiem for the Last God (55, capstone): melee 2, ranged 2, caster 2 --
+  deliberately exceeds what a Normal 3-hero party can ever fully satisfy
+  (needs 6 of 6 slots specifically filled); the true capstone is meant to
+  demand an actual Heroic/Mythic-sized roster, not just a leveled-up trio.
+  These are starting points, not final balance -- easy to retune per-raid
+  in the DevTool's Raids tab now that the infrastructure and the data are
+  both in place.
+
+**UI:** `RaidsPanel`'s existing role-mismatch warning line (from 0135)
+now sits alongside a second line when the party is both mismatched and
+at Heroic/Mythic, naming the actual cap ("Success can't rise above 65%
+at Heroic while unmet, no matter how strong the party is") rather than
+leaving the ceiling invisible until the player notices their preview
+number won't move.
+
+**Found and fixed along the way, not part of the original ask: the
+DevTool's Tuning tab could not save at all.** A real POST round-trip
+against the running devtool server (same verification convention 0135
+established) failed with 28 validation errors, all `"description" is
+required` -- every field belonging to 7 vendor-upgrade tuning groups
+(Smith's Discount, Bulk Scrapper, Apothecary's Discount, Arcane Discount,
+and the three Trade Favor variants: Blacksmith/Alchemist/Enchanter) was
+missing its required `description` string. This meant any edit to
+*any* tuning value, anywhere in the file, would have failed validation
+on save -- a real, pre-existing, full-tab-blocking bug, not something
+this patch introduced. Filled in all 28 with descriptions matching the
+exact phrasing convention every sibling `upgrade.*` entry already uses
+("Gold cost of X's first level.", "Multiplier applied per level to X's
+cost.", "Level cap for X.", "X's [effect] per level."). Re-verified with
+a real POST round-trip (against an isolated scratch copy of the data
+directory, not the live file, specifically to avoid the JSON
+re-serialization side effect described next) -- 368/368 entries save
+clean, and a deliberate negative test (stripping one entry's description
+back out) is correctly rejected.
+
+**A rough edge caught during that same verification, not left in the
+final diff -- same class of issue patch 0135 flagged before it, worth
+repeating the caution:** running the actual repo's `tuning.json` through
+a real server POST round-trip silently normalizes bare-float values like
+`2.0` to `2` (plain JS `JSON.stringify` behavior, harmless numerically,
+but an unrelated diff nobody asked for). Caught by diffing before
+finalizing; the description fields were instead inserted via direct
+targeted text edits against the original file, and all round-trip
+*verification* was performed against an isolated scratch copy of the
+data directory so the real file was never touched by the server's own
+save path at all.
+
+**Verified:** `npx tsc --noEmit` and a full `vite build` both clean;
+`node --check` on both devtool files; real GET/POST round-trips for
+both `raids` and `tuning` content types against an isolated scratch
+copy of `src/game/data/json/` (so the live files were never touched by
+the server's own re-serialization); a deliberate negative-validation
+test on the fixed `tuning` schema.
diff --git a/src/game/data/json/raids.json b/src/game/data/json/raids.json
index 516d4d6..e6584aa 100644
--- a/src/game/data/json/raids.json
+++ b/src/game/data/json/raids.json
@@ -19,6 +19,9 @@
     "description": "Something has been raising the dead again -- not one or two this time, but by the vaultful, row after row of them. Whatever's sealed inside is worth enough that a company of risen guards is still defending it centuries later, without pay, without rest, without ever once questioning the order. This isn't a job for one hero. Bring the whole guild.",
     "epilogue": "The vault held, in the end, for whoever the guild actually turned out to be. What was raised here has gone quiet again -- for now. Whatever tore open a road between the dead and the living to make this happen in the first place is still out there somewhere, and none of this so much as slowed it down.",
     "reqLevel": 22,
+    "requiredRoles": {
+      "caster": 1
+    },
     "encounterIds": [
       "bonewrought_outer_wardens",
       "bonewrought_choir_of_bones",
@@ -32,6 +35,11 @@
     "description": "The road the archive spoke of has no fixed heading and never did -- it opens for whoever means to walk it in a dying pilgrim's place, and closes the moment they stop meaning it. It has opened for this guild. Every guild that has taken this same walk before them left something behind to prove it: banners, hulls, ritual circles abandoned mid-rite, scattered across a shore that shouldn't exist on any chart and doesn't need to. Whatever's waiting at the end of that walk hasn't been walked in a very long time, and it does not entirely remember why anyone stopped.",
     "epilogue": "There is a difference between winning and being the one left standing when it is over. What the guild did at the end of that road wasn't a fight in the way every other fight has been a fight -- it was the last mile, walked the hard way, for something too far gone to make it easy. The guild is still deciding what to call that.",
     "reqLevel": 55,
+    "requiredRoles": {
+      "melee": 2,
+      "ranged": 2,
+      "caster": 2
+    },
     "encounterIds": [
       "last_god_field_of_failed_guilds",
       "last_god_silence_where_prayer_used_to_work",
@@ -46,6 +54,9 @@
     "description": "Something has been nesting in the high passes for longer than anyone noticed -- long enough to stop being one dragon and start being a brood. What used to be watched for avalanches gets watched for wings now, and lately there have been considerably more wings than there used to be.",
     "epilogue": "The brood is gone, and the ice it nested in is already creeping back over what's left of it -- which is, if nothing else, a tidier ending than Emberfell got. Nothing sings about a mountain that quietly stops being a problem. It just stops.",
     "reqLevel": 18,
+    "requiredRoles": {
+      "ranged": 1
+    },
     "encounterIds": [
       "wyrmkeep_hatchling_brood",
       "wyrmkeep_wyrm_keepers",
@@ -60,6 +71,10 @@
     "description": "The demon lord is gone, but the fortress he held is still standing empty -- gate broken, wards down, nobody left to guard whatever else might want it. Word reached the guild slowly: something else moved in behind him, in the confusion, and it isn't interested in ruling anything. It's using the ruin exactly the way it's always wanted to -- as somewhere quiet enough to finish a working nobody's stopped it from starting.",
     "epilogue": "The breach didn't open -- not all the way, not this time. What was trying to open it scattered the moment the guild reached the chamber, which either means it was never as unified as it looked, or means whatever's on the other side isn't done trying. Either way, the fortress is finally, actually empty. The guild isn't taking bets on how long that lasts.",
     "reqLevel": 26,
+    "requiredRoles": {
+      "melee": 1,
+      "caster": 1
+    },
     "encounterIds": [
       "gotout_trail_gone_cold",
       "gotout_the_last_loyal",
@@ -74,6 +89,10 @@
     "description": "A stretch of the eastern ridge has gone black with soot and quiet with fear -- something's nested up there, brought a whole clutch with it, and doesn't intend to be a passing problem the way most dragon sightings turn out to be.",
     "epilogue": "The nest is ash and empty shell now, the brood mother's watch finally over. Whatever drew her to that ridge in the first place is still a mystery nobody's especially eager to go looking for -- for now, the mountain's quiet again, and that's enough.",
     "reqLevel": 30,
+    "requiredRoles": {
+      "melee": 1,
+      "ranged": 1
+    },
     "encounterIds": [
       "blackdragon_the_keepers",
       "blackdragon_the_hatchlings",
@@ -87,6 +106,9 @@
     "description": "The ossuary the guild traced the lich to isn't a hideout so much as a workshop -- generations of the dead, sorted, stored, and put to use, all in service of a ritual that was never about raising an army in the first place.",
     "epilogue": "The lich is gone, his phylactery in pieces, and whatever he was trying to reach on the other side of that ritual didn't get through -- not this time. The guild is treating this as a delay, not an ending; something was listening on the other end of that ritual, and it's very unlikely to have forgotten the attempt.",
     "reqLevel": 41,
+    "requiredRoles": {
+      "caster": 2
+    },
     "requiresChainId": "hunt_a_lich",
     "encounterIds": [
       "hob_the_marrow_eater",
@@ -101,6 +123,11 @@
     "description": "There is no approach, no outer ring of guards, no lesser threat to clear first -- just a long walk down into the dark and, at the end of it, the thing itself, already aware the guild is coming and entirely unbothered by the fact.",
     "epilogue": "The Loom doesn't scream when it finally comes apart -- it just goes quiet, one borrowed voice at a time, until there's nothing left speaking through Millbrook at all. The town will need a long time to remember how to answer a question honestly again. That's a problem for people who aren't the guild to solve now.",
     "reqLevel": 43,
+    "requiredRoles": {
+      "melee": 1,
+      "ranged": 1,
+      "caster": 1
+    },
     "requiresChainId": "the_loom_beneath",
     "encounterIds": [
       "loom_the_presiding_intelligence"
diff --git a/src/game/data/json/tuning.json b/src/game/data/json/tuning.json
index 9722b3f..21e6093 100644
--- a/src/game/data/json/tuning.json
+++ b/src/game/data/json/tuning.json
@@ -143,6 +143,26 @@
     "max": 10,
     "description": "Multiplies gold/xp rewards at Mythic difficulty."
   },
+  {
+    "id": "raid_difficulty.heroic.roleMismatchCap",
+    "label": "Heroic: role mismatch success cap",
+    "category": "raid_difficulty",
+    "value": 65,
+    "default": 65,
+    "min": 5,
+    "max": 95,
+    "description": "Hard ceiling on final per-encounter success at Heroic while the party doesn't meet the raid's requiredRoles minimums. Layered on top of raid.roleMismatchPenaltyPerSlot, not a replacement for it -- gear/level can't push success above this no matter how far over the ordinary formula would otherwise go."
+  },
+  {
+    "id": "raid_difficulty.mythic.roleMismatchCap",
+    "label": "Mythic: role mismatch success cap",
+    "category": "raid_difficulty",
+    "value": 45,
+    "default": 45,
+    "min": 5,
+    "max": 95,
+    "description": "Same as Heroic's role mismatch success cap, deliberately lower -- Mythic is meant to punish an unmet role requirement harder than Heroic does, same relationship its successPenalty already has."
+  },
   {
     "id": "guild_facility.barracks.baseCost",
     "label": "Barracks: base cost",
@@ -3088,7 +3108,8 @@
     "category": "vendor_upgrades",
     "value": 300,
     "default": 300,
-    "min": 0
+    "min": 0,
+    "description": "Gold cost of Smith's Discount's first level."
   },
   {
     "id": "upgrade.smiths_discount.costGrowth",
@@ -3096,7 +3117,8 @@
     "category": "vendor_upgrades",
     "value": 1.8,
     "default": 1.8,
-    "min": 0
+    "min": 0,
+    "description": "Multiplier applied per level to Smith's Discount's cost."
   },
   {
     "id": "upgrade.smiths_discount.maxLevel",
@@ -3104,7 +3126,8 @@
     "category": "vendor_upgrades",
     "value": 6,
     "default": 6,
-    "min": 0
+    "min": 0,
+    "description": "Level cap for Smith's Discount."
   },
   {
     "id": "upgrade.smiths_discount.repairDiscountPerLevel",
@@ -3112,7 +3135,8 @@
     "category": "vendor_upgrades",
     "value": 5,
     "default": 5,
-    "min": 0
+    "min": 0,
+    "description": "Smith's Discount's repair-cost discount per level."
   },
   {
     "id": "upgrade.bulk_scrapper.baseCost",
@@ -3120,7 +3144,8 @@
     "category": "vendor_upgrades",
     "value": 700,
     "default": 700,
-    "min": 0
+    "min": 0,
+    "description": "Gold cost of Bulk Scrapper's first level."
   },
   {
     "id": "upgrade.bulk_scrapper.costGrowth",
@@ -3128,7 +3153,8 @@
     "category": "vendor_upgrades",
     "value": 1.9,
     "default": 1.9,
-    "min": 0
+    "min": 0,
+    "description": "Multiplier applied per level to Bulk Scrapper's cost."
   },
   {
     "id": "upgrade.bulk_scrapper.maxLevel",
@@ -3136,7 +3162,8 @@
     "category": "vendor_upgrades",
     "value": 5,
     "default": 5,
-    "min": 0
+    "min": 0,
+    "description": "Level cap for Bulk Scrapper."
   },
   {
     "id": "upgrade.bulk_scrapper.scrapBonusPerLevel",
@@ -3144,7 +3171,8 @@
     "category": "vendor_upgrades",
     "value": 8,
     "default": 8,
-    "min": 0
+    "min": 0,
+    "description": "Bulk Scrapper's scrap-yield bonus per level."
   },
   {
     "id": "upgrade.apothecary_discount.baseCost",
@@ -3152,7 +3180,8 @@
     "category": "vendor_upgrades",
     "value": 350,
     "default": 350,
-    "min": 0
+    "min": 0,
+    "description": "Gold cost of Apothecary's Discount's first level."
   },
   {
     "id": "upgrade.apothecary_discount.costGrowth",
@@ -3160,7 +3189,8 @@
     "category": "vendor_upgrades",
     "value": 1.85,
     "default": 1.85,
-    "min": 0
+    "min": 0,
+    "description": "Multiplier applied per level to Apothecary's Discount's cost."
   },
   {
     "id": "upgrade.apothecary_discount.maxLevel",
@@ -3168,7 +3198,8 @@
     "category": "vendor_upgrades",
     "value": 6,
     "default": 6,
-    "min": 0
+    "min": 0,
+    "description": "Level cap for Apothecary's Discount."
   },
   {
     "id": "upgrade.apothecary_discount.consumableDiscountPerLevel",
@@ -3176,7 +3207,8 @@
     "category": "vendor_upgrades",
     "value": 5,
     "default": 5,
-    "min": 0
+    "min": 0,
+    "description": "Apothecary's Discount's consumable price discount per level."
   },
   {
     "id": "upgrade.arcane_discount.baseCost",
@@ -3184,7 +3216,8 @@
     "category": "vendor_upgrades",
     "value": 400,
     "default": 400,
-    "min": 0
+    "min": 0,
+    "description": "Gold cost of Arcane Discount's first level."
   },
   {
     "id": "upgrade.arcane_discount.costGrowth",
@@ -3192,7 +3225,8 @@
     "category": "vendor_upgrades",
     "value": 1.85,
     "default": 1.85,
-    "min": 0
+    "min": 0,
+    "description": "Multiplier applied per level to Arcane Discount's cost."
   },
   {
     "id": "upgrade.arcane_discount.maxLevel",
@@ -3200,7 +3234,8 @@
     "category": "vendor_upgrades",
     "value": 6,
     "default": 6,
-    "min": 0
+    "min": 0,
+    "description": "Level cap for Arcane Discount."
   },
   {
     "id": "upgrade.arcane_discount.enchantDiscountPerLevel",
@@ -3208,7 +3243,8 @@
     "category": "vendor_upgrades",
     "value": 5,
     "default": 5,
-    "min": 0
+    "min": 0,
+    "description": "Arcane Discount's enchant price discount per level."
   },
   {
     "id": "upgrade.trade_favor_blacksmith.baseCost",
@@ -3216,7 +3252,8 @@
     "category": "vendor_upgrades",
     "value": 900,
     "default": 900,
-    "min": 0
+    "min": 0,
+    "description": "Gold cost of Trade Favor (Blacksmith)'s first level."
   },
   {
     "id": "upgrade.trade_favor_blacksmith.costGrowth",
@@ -3224,7 +3261,8 @@
     "category": "vendor_upgrades",
     "value": 2.0,
     "default": 2.0,
-    "min": 0
+    "min": 0,
+    "description": "Multiplier applied per level to Trade Favor (Blacksmith)'s cost."
   },
   {
     "id": "upgrade.trade_favor_blacksmith.maxLevel",
@@ -3232,7 +3270,8 @@
     "category": "vendor_upgrades",
     "value": 3,
     "default": 3,
-    "min": 0
+    "min": 0,
+    "description": "Level cap for Trade Favor (Blacksmith)."
   },
   {
     "id": "upgrade.trade_favor_blacksmith.vendorFreeRerollsPerLevel",
@@ -3240,7 +3279,8 @@
     "category": "vendor_upgrades",
     "value": 1,
     "default": 1,
-    "min": 0
+    "min": 0,
+    "description": "Trade Favor (Blacksmith)'s free vendor rerolls per day granted per level."
   },
   {
     "id": "upgrade.trade_favor_alchemist.baseCost",
@@ -3248,7 +3288,8 @@
     "category": "vendor_upgrades",
     "value": 900,
     "default": 900,
-    "min": 0
+    "min": 0,
+    "description": "Gold cost of Trade Favor (Alchemist)'s first level."
   },
   {
     "id": "upgrade.trade_favor_alchemist.costGrowth",
@@ -3256,7 +3297,8 @@
     "category": "vendor_upgrades",
     "value": 2.0,
     "default": 2.0,
-    "min": 0
+    "min": 0,
+    "description": "Multiplier applied per level to Trade Favor (Alchemist)'s cost."
   },
   {
     "id": "upgrade.trade_favor_alchemist.maxLevel",
@@ -3264,7 +3306,8 @@
     "category": "vendor_upgrades",
     "value": 3,
     "default": 3,
-    "min": 0
+    "min": 0,
+    "description": "Level cap for Trade Favor (Alchemist)."
   },
   {
     "id": "upgrade.trade_favor_alchemist.vendorFreeRerollsPerLevel",
@@ -3272,7 +3315,8 @@
     "category": "vendor_upgrades",
     "value": 1,
     "default": 1,
-    "min": 0
+    "min": 0,
+    "description": "Trade Favor (Alchemist)'s free vendor rerolls per day granted per level."
   },
   {
     "id": "upgrade.trade_favor_enchanter.baseCost",
@@ -3280,7 +3324,8 @@
     "category": "vendor_upgrades",
     "value": 900,
     "default": 900,
-    "min": 0
+    "min": 0,
+    "description": "Gold cost of Trade Favor (Enchanter)'s first level."
   },
   {
     "id": "upgrade.trade_favor_enchanter.costGrowth",
@@ -3288,7 +3333,8 @@
     "category": "vendor_upgrades",
     "value": 2.0,
     "default": 2.0,
-    "min": 0
+    "min": 0,
+    "description": "Multiplier applied per level to Trade Favor (Enchanter)'s cost."
   },
   {
     "id": "upgrade.trade_favor_enchanter.maxLevel",
@@ -3296,7 +3342,8 @@
     "category": "vendor_upgrades",
     "value": 3,
     "default": 3,
-    "min": 0
+    "min": 0,
+    "description": "Level cap for Trade Favor (Enchanter)."
   },
   {
     "id": "upgrade.trade_favor_enchanter.vendorFreeRerollsPerLevel",
@@ -3304,7 +3351,8 @@
     "category": "vendor_upgrades",
     "value": 1,
     "default": 1,
-    "min": 0
+    "min": 0,
+    "description": "Trade Favor (Enchanter)'s free vendor rerolls per day granted per level."
   },
   {
     "id": "role.unlockBaseCost",
diff --git a/src/game/data/raids.ts b/src/game/data/raids.ts
index fb4d52e..25b68d4 100644
--- a/src/game/data/raids.ts
+++ b/src/game/data/raids.ts
@@ -58,6 +58,14 @@ export const RAID_DIFFICULTIES: Record<RaidDifficulty, RaidDifficultyConfig> = {
   // literal here. Heroic/Mythic's four fields each read from the tuning
   // registry instead -- editable live via the devtool's Tuning tab. See
   // tuning.ts and tuning.json.
+  // roleMismatchCap: Normal deliberately has none at all (undefined, not
+  // just a high number) -- a mismatched Normal party still only eats the
+  // ordinary per-slot roleMismatchPenalty subtraction and can climb back
+  // up to MAX_SUCCESS on gear/level alone, same as before this existed.
+  // Heroic/Mythic read theirs from the tuning registry like their other
+  // three fields -- Mythic's is deliberately the lower of the two, same
+  // "genuinely brutal, not just harder than Heroic" intent as its
+  // successPenalty above.
   normal: { difficulty: 'normal', partySize: 3, successPenalty: 0, rewardMultiplier: 1, lootBonus: 0, durationMultiplier: 1 },
   heroic: {
     difficulty: 'heroic', partySize: 6,
@@ -65,6 +73,7 @@ export const RAID_DIFFICULTIES: Record<RaidDifficulty, RaidDifficultyConfig> = {
     rewardMultiplier: Tuning.get('raid_difficulty.heroic.rewardMultiplier'),
     lootBonus: Tuning.get('raid_difficulty.heroic.lootBonus'),
     durationMultiplier: Tuning.get('raid_difficulty.heroic.durationMultiplier'),
+    roleMismatchCap: Tuning.get('raid_difficulty.heroic.roleMismatchCap'),
   },
   mythic: {
     difficulty: 'mythic', partySize: 9,
@@ -72,6 +81,7 @@ export const RAID_DIFFICULTIES: Record<RaidDifficulty, RaidDifficultyConfig> = {
     rewardMultiplier: Tuning.get('raid_difficulty.mythic.rewardMultiplier'),
     lootBonus: Tuning.get('raid_difficulty.mythic.lootBonus'),
     durationMultiplier: Tuning.get('raid_difficulty.mythic.durationMultiplier'),
+    roleMismatchCap: Tuning.get('raid_difficulty.mythic.roleMismatchCap'),
   },
 };
 
diff --git a/src/game/managers/RaidManager.ts b/src/game/managers/RaidManager.ts
index d95ee25..f9221f6 100644
--- a/src/game/managers/RaidManager.ts
+++ b/src/game/managers/RaidManager.ts
@@ -105,6 +105,21 @@ export const RaidManager = {
     return unmet * Tuning.get('raid.roleMismatchPenaltyPerSlot');
   },
 
+  /**
+   * True if the party is missing at least one requiredRoles slot -- kept
+   * as its own boolean rather than reusing `roleMismatchPenalty(...) > 0`,
+   * so the Heroic/Mythic success ceiling below still engages even if
+   * `raid.roleMismatchPenaltyPerSlot` itself were ever tuned down to 0.
+   * The two are meant to be independently tunable: one is "how much does
+   * each unmet slot cost you," the other is "how high can you climb back
+   * to regardless" -- see RaidDifficultyConfig.roleMismatchCap.
+   */
+  hasRoleMismatch(heroes: Hero[], requiredRoles?: Partial<Record<Role, number>>): boolean {
+    if (!requiredRoles) return false;
+    const counts = RaidManager.partyRoleCounts(heroes);
+    return (Object.entries(requiredRoles) as [Role, number][]).some(([role, needed]) => counts[role] < needed);
+  },
+
   /**
    * Economy contributions (gold/xp/loot/speed) use a plain average across
    * the party instead -- these aren't a pass/fail gate the way success is,
@@ -151,11 +166,19 @@ export const RaidManager = {
     if (!raid || !encounter) return 0;
     const heroes = heroIds.map((id) => state.heroes.find((h) => h.id === id)).filter((h): h is Hero => !!h);
     const bonus = RaidManager.partySuccessBonus(state, heroes, now, raid.reqLevel);
-    const penalty = RAID_DIFFICULTIES[difficulty].successPenalty;
+    const diffCfg = RAID_DIFFICULTIES[difficulty];
     const elemental = RaidManager.elementalBonus(heroes, encounter);
     const override = raid.successModifier ?? 0;
     const roleMismatch = RaidManager.roleMismatchPenalty(heroes, raid.requiredRoles);
-    return clamp(encounter.baseSuccess - penalty + bonus + elemental + override - roleMismatch, MIN_SUCCESS, MAX_SUCCESS);
+    const success = clamp(encounter.baseSuccess - diffCfg.successPenalty + bonus + elemental + override - roleMismatch, MIN_SUCCESS, MAX_SUCCESS);
+    // Heroic/Mythic's role-mismatch ceiling applies AFTER the ordinary
+    // clamp, only while the party is actually missing a required slot --
+    // see RaidDifficultyConfig.roleMismatchCap. Normal has no cap
+    // (undefined), so this is a no-op there regardless of party makeup.
+    if (diffCfg.roleMismatchCap != null && RaidManager.hasRoleMismatch(heroes, raid.requiredRoles)) {
+      return Math.min(success, diffCfg.roleMismatchCap);
+    }
+    return success;
   },
 
   /**
@@ -258,6 +281,13 @@ export const RaidManager = {
     const rng = createRng(`raid:${active.raidId}:${active.difficulty}:${active.startedAt}`);
     const heroes = active.heroIds.map((id) => state.heroes.find((h) => h.id === id)).filter((h): h is Hero => !!h);
     const economy = RaidManager.partyEconomyMods(state, heroes, resolvedAt);
+    // The party is locked in for the whole raid -- requiredRoles lives on
+    // RaidDef, not per-encounter, and nothing here lets heroes swap roles
+    // mid-raid. Computed once, same as active.partySuccessBonus itself
+    // (which already has roleMismatchPenalty folded in from start()) --
+    // every encounter in the loop below reuses this single result rather
+    // than re-deriving it per encounter.
+    const roleMismatched = RaidManager.hasRoleMismatch(heroes, raid?.requiredRoles);
 
     let encountersCleared = 0;
     let gold = 0;
@@ -271,7 +301,14 @@ export const RaidManager = {
       if (!encounter) continue; // devtool data drift safety -- an unknown id is skipped, not a crash
       const elemental = RaidManager.elementalBonus(heroes, encounter);
       const override = raid?.successModifier ?? 0;
-      const chance = clamp(encounter.baseSuccess - diffCfg.successPenalty + active.partySuccessBonus + elemental + override, MIN_SUCCESS, MAX_SUCCESS);
+      const rawChance = clamp(encounter.baseSuccess - diffCfg.successPenalty + active.partySuccessBonus + elemental + override, MIN_SUCCESS, MAX_SUCCESS);
+      // Same Heroic/Mythic role-mismatch ceiling as previewEncounterSuccess,
+      // applied identically here so the actual roll never has better odds
+      // than what the party saw in the preview -- see
+      // RaidDifficultyConfig.roleMismatchCap.
+      const chance = (diffCfg.roleMismatchCap != null && roleMismatched)
+        ? Math.min(rawChance, diffCfg.roleMismatchCap)
+        : rawChance;
       if (!rng.chance(chance)) break;
 
       encountersCleared += 1;
diff --git a/src/game/types.ts b/src/game/types.ts
index ae8edae..f33559f 100644
--- a/src/game/types.ts
+++ b/src/game/types.ts
@@ -914,6 +914,22 @@ export interface RaidDifficultyConfig {
    * Raid Upgrades), not inherit quest ones for free.
    */
   durationMultiplier: number;
+  /**
+   * A hard ceiling on final per-encounter success while the party doesn't
+   * meet a raid's `requiredRoles` minimums -- undefined (Normal) means no
+   * ceiling at all, so a mismatched-but-overleveled-and-geared party can
+   * still climb all the way to MAX_SUCCESS the same as today. Heroic and
+   * Mythic define one via the tuning registry instead: no matter how far
+   * gear/level push the raw computed success, an unmet role requirement
+   * caps it there. This is layered ON TOP of roleMismatchPenalty (the
+   * existing flat per-unmet-slot subtraction), not a replacement for it --
+   * the subtraction still applies and still scales with how many slots
+   * are unmet; the cap only ever pulls a result *down* to itself, never up,
+   * and only takes effect once the party is actually missing something.
+   * See RaidManager.hasRoleMismatch / the "Role mismatch success ceiling"
+   * writeup in guild-idler-status.md for the full reasoning.
+   */
+  roleMismatchCap?: number;
 }
 
 /**
diff --git a/src/ui/panels/RaidsPanel.tsx b/src/ui/panels/RaidsPanel.tsx
index 83609e0..257726a 100644
--- a/src/ui/panels/RaidsPanel.tsx
+++ b/src/ui/panels/RaidsPanel.tsx
@@ -454,6 +454,8 @@ function RaidDetailModal({
                   .filter((h): h is Hero => !!h);
                 const counts = RaidManager.partyRoleCounts(previewHeroes);
                 const penalty = RaidManager.roleMismatchPenalty(previewHeroes, raid.requiredRoles);
+                const mismatched = RaidManager.hasRoleMismatch(previewHeroes, raid.requiredRoles);
+                const cap = cfg.roleMismatchCap;
                 return (
                   <div style={{ marginTop: 6, marginBottom: 6 }}>
                     <div className="row" style={{ gap: 8 }}>
@@ -466,6 +468,11 @@ function RaidDetailModal({
                         Role mix unmet -- {'-'}{penalty}% success this run until it's covered.
                       </p>
                     )}
+                    {mismatched && cap != null && (
+                      <p className="tiny bad" style={{ marginTop: 2 }}>
+                        Success can't rise above {cap}% at {difficulty[0].toUpperCase()}{difficulty.slice(1)} while unmet, no matter how strong the party is.
+                      </p>
+                    )}
                   </div>
                 );
               })()}
