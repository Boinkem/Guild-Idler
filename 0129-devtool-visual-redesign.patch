diff --git a/guild-idler-status.md b/guild-idler-status.md
index 3e1c36d..568ca17 100644
--- a/guild-idler-status.md
+++ b/guild-idler-status.md
@@ -5057,6 +5057,65 @@ actually new) -- clean pass. The patch itself was verified with a real
 confirming it applies cleanly and produces byte-identical output to what
 was tested here.
 
+### DevTool visual redesign -- built
+Requested a visual upgrade to the DevTool via Claude Design, asked what
+files/prompt to hand it (recommended `style.css` + `index.html` + real
+screenshots of the running tool, since `app.js` generates all markup
+dynamically via JS template strings -- a design-focused tool gets
+nothing useful from reading 79KB of vanilla-JS render logic, and can't
+infer real on-screen layout from code alone). Result came back as a new
+`style.css`: cooler near-black palette off the previous muddy purple,
+brass survives as the one warm accent; higher density (smaller tab
+chips, tighter table rows); fills replaced with borders/underlines
+where a fill was only ever signalling state (active tab, selected
+patch), cutting visual noise per screenful. Explicitly built as a
+reskin, not a rebuild -- same selectors, same markup contract, zero
+`app.js` changes required, matching exactly what was asked for.
+
+**Verified the reskin claim, not just trusted it.** A prompt asking an
+LLM to "keep the same selectors" doesn't guarantee it did -- checked
+directly: extracted every class/id selector from both the old and new
+stylesheets and diffed them. Zero classes or ids present in the old
+file are missing from the new one -- full backward coverage, no
+regressions. Went a level further and cross-referenced against what
+`app.js` *actually generates* at runtime (every `class="..."` template
+string, every `classList.add/toggle`, every dynamic `.className`
+assignment) rather than just diffing stylesheet-to-stylesheet -- 95
+distinct classes in real use, checked each one.
+
+**Found a handful of classes app.js generates that neither the old nor
+the new stylesheet styles** (`.spread`, `.section-heading`,
+`.tuning-value-wrap`, and the bare `.egg-reward`/`.result-gem`/
+`.loot-field` wrapper divs, plus a `.clean` state class on the Patches
+tab's git-status block that only ever gets the unstyled default while
+its sibling `.dirty` gets a real highlight). Confirmed each one
+individually against the OLD file before concluding anything -- every
+single one was already unstyled before this redesign too, so none of
+these are regressions the reskin introduced. Left alone rather than
+silently patched in on the side, since fixing them wasn't what was
+asked for this pass and a couple (`.spread`, `.section-heading`) look
+like they were probably meant to have real styling from the start --
+worth a small, explicitly-scoped follow-up rather than sneaking extra
+changes into a redesign patch.
+
+Also confirmed: brace-balanced (185 open/185 close, no syntax slip),
+and every CSS custom property `app.js` itself references inline via
+`style="...var(--x)..."` (`--brass`/`--muted`/`--panel2`/`--panel3`/
+`--text`) still exists in the new `:root` block -- a renamed or dropped
+variable here would have broken inline styles invisibly, not thrown an
+error.
+
+**Not verified:** an actual rendered screenshot. Started the real
+DevTool server with the new stylesheet applied and confirmed it serves
+correctly over HTTP, but Playwright's Chromium couldn't reach
+`localhost` from inside this sandbox (a browser-subprocess network
+restriction, not a dead server -- same environment limitation noted in
+an earlier session's own verification writeup). The selector-coverage
+and variable-resolution checks above are real and thorough, but a
+human eyeballing the actual rendered tool before considering this fully
+closed is still worth doing, same as any other "verified everything but
+the pixels" patch.
+
 ### DevTool clarity pass: unit hints + two real save-blocking bugs found -- built
 Direct request: the generic mods/stats/effect/eventEffects editor (a
 compact label+input grid, shared across equipment/consumables/injuries/
diff --git a/tools/devtool/public/style.css b/tools/devtool/public/style.css
index ec8747b..031129f 100644
--- a/tools/devtool/public/style.css
+++ b/tools/devtool/public/style.css
@@ -1,158 +1,359 @@
+/* =============================================================================
+   Guildbound Dev Tool — stylesheet
+   Visual upgrade pass. Same selectors, same markup contract as before:
+   nothing here requires a change to app.js.
+
+   Notes on the pass:
+   - Palette shifted off the muddy purple toward a cool near-black with one
+     warm accent (brass). Purple survives only as a faint hue in the greys.
+   - Density raised: smaller tab chips, tighter rows, sticky table head.
+   - Fills replaced with borders/underlines where a fill was only signalling
+     state (active tab, selected patch) — less visual noise per screenful.
+   ========================================================================== */
+
 :root {
-  --bg: #171320; --panel: #221c2e; --panel2: #2b2338; --panel3: #362c46;
-  --edge: #0e0b14; --text: #f3e6c8; --muted: #a294b5;
-  --brass: #d9a441; --good: #79a86b; --bad: #a33a3a; --blue: #5b8fd6;
-  font-family: ui-monospace, 'Cascadia Mono', Menlo, Consolas, monospace;
+  /* Surfaces: each step is a real, visible tick lighter than the last. */
+  --bg: #131218;
+  --panel: #1a1922;
+  --panel2: #23212d;
+  --panel3: #333040;
+  --edge: #0a090e;
+
+  /* Text */
+  --text: #e6e1d6;
+  --text-bright: #fbf7ef;
+  --muted: #8e88a0;
+  --dim: #6a6479;
+
+  /* Accents — one warm, three semantic. Same chroma family. */
+  --brass: #e0a94a;
+  --brass-dim: #8a6a2b;
+  --brass-wash: rgba(224, 169, 74, 0.12);
+  --good: #6fae72;
+  --bad: #cf5a5a;
+  --bad-wash: rgba(207, 90, 90, 0.12);
+  --blue: #6b9ede;
+
+  /* --topbar-h: 112px;  <- uncomment/set to enable sticky table headers */
+  --radius: 4px;
+  --focus: 0 0 0 2px rgba(224, 169, 74, 0.45);
+
+  font-family: ui-monospace, 'Cascadia Mono', 'JetBrains Mono', Menlo, Consolas, monospace;
+  color-scheme: dark;
 }
+
 * { box-sizing: border-box; }
-body { margin: 0; background: var(--bg); color: var(--text); font-size: 13px; }
+
+body {
+  margin: 0;
+  background: var(--bg);
+  color: var(--text);
+  font-size: 13px;
+  line-height: 1.45;
+  -webkit-font-smoothing: antialiased;
+}
+
+a { color: var(--brass); text-decoration: none; border-bottom: 1px solid var(--brass-dim); }
+a:hover { color: var(--text-bright); border-bottom-color: currentColor; }
+
+::selection { background: var(--brass-dim); color: var(--text-bright); }
+
+/* Scrollbars — dark, thin, out of the way. */
+* { scrollbar-width: thin; scrollbar-color: var(--panel3) transparent; }
+*::-webkit-scrollbar { width: 10px; height: 10px; }
+*::-webkit-scrollbar-thumb { background: var(--panel3); border-radius: 6px; border: 2px solid var(--bg); }
+*::-webkit-scrollbar-thumb:hover { background: #45415a; }
+*::-webkit-scrollbar-track { background: transparent; }
+
+:focus-visible { outline: none; box-shadow: var(--focus); }
+
+/* ------------------------------- topbar ---------------------------------- */
 
 .topbar {
-  display: flex; align-items: center; gap: 20px;
-  padding: 10px 16px; background: var(--panel2); border-bottom: 1px solid var(--edge);
+  display: flex; align-items: center; gap: 18px;
+  padding: 9px 14px;
+  background: linear-gradient(var(--panel2), var(--panel));
+  border-bottom: 1px solid var(--edge);
+  box-shadow: 0 1px 0 rgba(255, 255, 255, 0.04) inset, 0 2px 10px rgba(0, 0, 0, 0.4);
   position: sticky; top: 0; z-index: 5;
 }
-.topbar h1 { font-size: 15px; margin: 0; white-space: nowrap; }
-.topbar h1 span { color: var(--brass); font-weight: normal; margin-left: 6px; }
-#tabs { display: flex; gap: 4px; flex: 1; flex-wrap: wrap; }
+.topbar h1 {
+  font-size: 13px; margin: 0; white-space: nowrap; letter-spacing: 0.02em;
+  color: var(--text-bright); font-weight: 700; align-self: flex-start; padding-top: 3px;
+}
+.topbar h1 span {
+  color: var(--brass); font-weight: normal; margin-left: 6px;
+  font-size: 11px; text-transform: uppercase; letter-spacing: 0.14em;
+}
+
+#tabs { display: flex; gap: 3px; flex: 1; flex-wrap: wrap; }
 #tabs button {
-  background: var(--panel); border: 1px solid var(--edge); color: var(--text);
-  padding: 7px 12px; cursor: pointer; font: inherit;
+  background: transparent;
+  border: 1px solid transparent;
+  border-radius: var(--radius);
+  color: var(--muted);
+  padding: 4px 9px;
+  font: inherit; font-size: 12px; line-height: 1.5;
+  white-space: nowrap;
+  cursor: pointer;
+  transition: color 90ms, background 90ms, border-color 90ms;
 }
-#tabs button.active { background: var(--brass); color: #241a0a; font-weight: bold; }
-.topbar-status { font-size: 11px; color: var(--muted); min-width: 160px; text-align: right; }
+#tabs button:hover { color: var(--text); background: rgba(255, 255, 255, 0.05); }
+#tabs button.active {
+  background: var(--brass-wash);
+  border-color: var(--brass-dim);
+  color: var(--brass);
+  font-weight: 700;
+  box-shadow: 0 0 0 1px rgba(224, 169, 74, 0.08);
+}
+
+.topbar-status { font-size: 11px; color: var(--muted); min-width: 150px; text-align: right; align-self: flex-start; padding-top: 4px; }
 .topbar-status.err { color: var(--bad); }
 .topbar-status.ok { color: var(--good); }
 
-main { padding: 18px; max-width: 1100px; margin: 0 auto; }
+main { padding: 20px 22px 60px; max-width: 1440px; margin: 0 auto; }
+
+main > h2, main > h3 { color: var(--text-bright); font-size: 14px; margin: 22px 0 4px; letter-spacing: 0.01em; }
+main > p { color: var(--muted); font-size: 12px; margin: 0 0 12px; max-width: 90ch; text-wrap: pretty; }
+
+/* ------------------------------- controls -------------------------------- */
 
-.toolbar { display: flex; gap: 8px; margin-bottom: 14px; align-items: center; }
+.toolbar { display: flex; gap: 8px; margin-bottom: 12px; align-items: center; }
 .toolbar .spacer { flex: 1; }
 
 button {
-  background: var(--panel2); border: 1px solid var(--edge); color: var(--text);
-  padding: 7px 12px; cursor: pointer; font: inherit;
-}
-button:hover { background: var(--panel3); }
-button.primary { background: #8c6a2a; border-color: var(--brass); color: #fff8e6; }
-button.primary:hover { background: var(--brass); color: #241a0a; }
-button.danger { border-color: var(--bad); }
-button.danger:hover { background: var(--bad); }
-button:disabled { opacity: 0.4; cursor: not-allowed; }
-
-table { width: 100%; border-collapse: collapse; margin-bottom: 20px; }
-th, td { text-align: left; padding: 7px 8px; border-bottom: 1px solid var(--panel3); font-size: 12px; }
-th { color: var(--muted); font-weight: normal; text-transform: uppercase; font-size: 10px; letter-spacing: 0.05em; }
-tr:hover td { background: var(--panel); }
-td.actions { white-space: nowrap; width: 1%; }
-td.actions button { padding: 4px 8px; font-size: 11px; margin-right: 4px; }
-
-.rarity-common { color: #b9ad93; } .rarity-uncommon { color: var(--good); }
-.rarity-rare { color: var(--blue); } .rarity-epic { color: #a874d6; } .rarity-legendary { color: var(--brass); }
-.kind-positive { color: var(--good); } .kind-negative { color: var(--bad); } .kind-neutral { color: var(--muted); }
+  background: var(--panel2);
+  border: 1px solid var(--panel3);
+  border-radius: var(--radius);
+  color: var(--text);
+  padding: 6px 11px;
+  white-space: nowrap;
+  cursor: pointer;
+  font: inherit; font-size: 12px;
+  transition: background 90ms, border-color 90ms, color 90ms;
+}
+button:hover { background: var(--panel3); border-color: #45415a; color: var(--text-bright); }
+button:active { transform: translateY(1px); }
+
+button.primary {
+  background: var(--brass-wash); border-color: var(--brass-dim); color: var(--brass); font-weight: 700;
+}
+button.primary:hover { background: var(--brass); border-color: var(--brass); color: #1d1608; }
+
+button.danger { border-color: rgba(207, 90, 90, 0.45); color: #e0928f; background: transparent; }
+button.danger:hover { background: var(--bad-wash); border-color: var(--bad); color: #ffd6d3; }
+
+button:disabled, button:disabled:hover {
+  opacity: 0.35; cursor: not-allowed; background: var(--panel2);
+  border-color: var(--panel3); color: var(--muted); transform: none;
+}
+
+/* -------------------------------- tables --------------------------------- */
+
+table {
+  width: 100%; border-collapse: separate; border-spacing: 0;
+  margin-bottom: 20px;
+  background: var(--panel);
+  border: 1px solid var(--panel3);
+  border-radius: var(--radius);
+  overflow: hidden;
+}
+th, td {
+  text-align: left; padding: 6px 10px; font-size: 12px;
+  border-bottom: 1px solid rgba(255, 255, 255, 0.045);
+}
+/* Header row can stick under the topbar, but only if it knows how tall the
+   topbar is -- the tab wall wraps to a different number of rows per viewport.
+   Off by default; set --topbar-h on :root (or on body from app.js, one line:
+   document.body.style.setProperty('--topbar-h', topbar.offsetHeight + 'px'))
+   to turn it on. */
+th {
+  position: sticky; top: var(--topbar-h, 0px); z-index: 2;
+  background: var(--panel2);
+  color: var(--muted); font-weight: 600;
+  text-transform: uppercase; font-size: 10px; letter-spacing: 0.09em;
+  padding-top: 8px; padding-bottom: 8px;
+  border-bottom: 1px solid var(--panel3);
+  white-space: nowrap;
+}
+tbody tr:nth-child(even) td { background: rgba(255, 255, 255, 0.018); }
+tbody tr:last-child td { border-bottom: none; }
+tbody tr:hover td { background: var(--panel2); }
+tbody tr:hover td:first-child { box-shadow: inset 2px 0 0 var(--brass); }
+
+/* Numbers read as columns of digits, not words. */
+td { font-variant-numeric: tabular-nums; }
+tbody td:first-child, td.icon-cell + td { color: var(--text-bright); }
+
+td.actions { white-space: nowrap; width: 1%; text-align: right; }
+td.actions button {
+  padding: 3px 8px; font-size: 11px; margin-left: 4px; margin-right: 0;
+  background: transparent; border-color: var(--panel3); color: var(--muted);
+}
+td.actions button:hover { background: var(--panel3); color: var(--text-bright); }
+td.actions button.danger { color: #b87b78; }
+td.actions button.danger:hover { background: var(--bad-wash); border-color: var(--bad); color: #ffd6d3; }
+
+.rarity-common { color: #a49b8a; }
+.rarity-uncommon { color: var(--good); }
+.rarity-rare { color: var(--blue); }
+.rarity-epic { color: #b183e0; }
+.rarity-legendary { color: var(--brass); }
+.kind-positive { color: var(--good); }
+.kind-negative { color: var(--bad); }
+.kind-neutral { color: var(--muted); }
+
+/* --------------------------------- modal --------------------------------- */
 
 .editor-overlay {
-  position: fixed; inset: 0; background: rgba(10, 8, 14, 0.82);
-  display: flex; align-items: flex-start; justify-content: center; padding: 40px 20px;
-  overflow-y: auto; z-index: 10;
+  position: fixed; inset: 0;
+  background: rgba(8, 7, 11, 0.78);
+  backdrop-filter: blur(2px);
+  display: flex; align-items: flex-start; justify-content: center;
+  padding: 40px 20px; overflow-y: auto; z-index: 10;
 }
 .editor {
-  background: var(--panel); border: 1px solid var(--brass); padding: 20px;
-  width: 100%; max-width: 560px;
+  background: var(--panel);
+  border: 1px solid var(--panel3);
+  border-top: 2px solid var(--brass);
+  border-radius: var(--radius);
+  box-shadow: 0 24px 60px rgba(0, 0, 0, 0.6);
+  padding: 18px 20px 20px;
+  width: 100%; max-width: 620px;
 }
-.editor h2 { margin-top: 0; font-size: 14px; color: var(--brass); }
+.editor h2 {
+  margin: 0 0 16px; padding-bottom: 10px;
+  font-size: 13px; color: var(--brass);
+  text-transform: uppercase; letter-spacing: 0.1em;
+  border-bottom: 1px solid var(--panel3);
+}
+
 .field { margin-bottom: 12px; }
-.field label { display: block; font-size: 11px; color: var(--muted); margin-bottom: 4px; }
+.field label {
+  display: block; font-size: 10px; color: var(--muted);
+  text-transform: uppercase; letter-spacing: 0.08em; margin-bottom: 4px;
+}
 .field input[type=text], .field input[type=number], .field textarea, .field select {
-  width: 100%; background: var(--panel2); border: 1px solid var(--panel3); color: var(--text);
-  padding: 7px 8px; font: inherit;
+  width: 100%;
+  background: var(--bg);
+  border: 1px solid var(--panel3);
+  border-radius: var(--radius);
+  color: var(--text-bright);
+  padding: 6px 8px; font: inherit; font-size: 12px;
+  transition: border-color 90ms, box-shadow 90ms;
 }
-.field textarea { min-height: 50px; resize: vertical; }
-.field .hint { font-size: 10px; color: var(--muted); margin-top: 3px; }
+.field input:hover, .field textarea:hover, .field select:hover { border-color: #45415a; }
+.field input:focus, .field textarea:focus, .field select:focus {
+  outline: none; border-color: var(--brass-dim); box-shadow: var(--focus);
+}
+.field input::placeholder, .field textarea::placeholder { color: var(--dim); }
+.field textarea { min-height: 54px; resize: vertical; line-height: 1.5; }
+.field .hint { font-size: 10px; color: var(--dim); margin-top: 4px; }
 .field-row { display: grid; grid-template-columns: repeat(auto-fit, minmax(140px, 1fr)); gap: 10px; }
+
 .list-input { display: flex; flex-direction: column; gap: 4px; }
 .list-input .row { display: flex; gap: 4px; }
 .list-input input { flex: 1; }
 .list-input .remove { padding: 4px 8px; }
+
 .kv-grid { display: grid; grid-template-columns: 1fr 90px; gap: 6px 8px; align-items: center; }
-.kv-grid label { font-size: 11px; color: var(--muted); }
+.kv-grid label { font-size: 11px; color: var(--muted); text-transform: none; letter-spacing: 0; margin: 0; }
+
+.editor-actions {
+  display: flex; gap: 8px; justify-content: flex-end;
+  margin: 18px -20px -20px; padding: 14px 20px;
+  background: var(--panel2);
+  border-top: 1px solid var(--panel3);
+  border-radius: 0 0 var(--radius) var(--radius);
+}
+
+.errors {
+  background: var(--bad-wash); border: 1px solid rgba(207, 90, 90, 0.5);
+  border-left: 3px solid var(--bad); border-radius: var(--radius);
+  padding: 10px 12px; margin-bottom: 14px; font-size: 12px; color: #f0bdba;
+}
+.errors ul { margin: 6px 0 0; padding-left: 18px; }
 
-/* ----------------------------- chain stages -------------------------------- */
+.empty { color: var(--dim); font-size: 12px; padding: 28px 0; text-align: center; }
+
+/* ----------------------------- chain stages ------------------------------ */
 /* Each stage is its own bordered mini-form, not just another list-input row --
    6 fields per stage (name/flavour/tag/difficulty/duration/gold), too much for
    a single-line row. */
-.stages-input { display: flex; flex-direction: column; gap: 12px; }
-.stage-row { background: var(--panel2); border: 1px solid var(--panel3); padding: 10px 12px; }
+.stages-input { display: flex; flex-direction: column; gap: 10px; }
+.stage-row {
+  background: var(--panel2); border: 1px solid var(--panel3);
+  border-radius: var(--radius); padding: 10px 12px;
+}
 .stage-row-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 8px; }
-.stage-row-title { font-size: 11px; color: var(--muted); text-transform: uppercase; letter-spacing: 0.05em; }
-.stage-row label { display: block; font-size: 11px; color: var(--muted); margin: 6px 0 4px; }
+.stage-row-title { font-size: 10px; color: var(--brass); text-transform: uppercase; letter-spacing: 0.1em; }
+.stage-row label {
+  display: block; font-size: 10px; color: var(--muted);
+  text-transform: uppercase; letter-spacing: 0.07em; margin: 6px 0 4px;
+}
 .stage-row input[type=text], .stage-row textarea, .stage-row select, .stage-row input[type=number] {
-  width: 100%; background: var(--panel); border: 1px solid var(--panel3); color: var(--text);
-  padding: 6px 7px; font: inherit;
+  width: 100%; background: var(--bg); border: 1px solid var(--panel3); border-radius: var(--radius);
+  color: var(--text-bright); padding: 5px 7px; font: inherit; font-size: 12px;
+}
+.stage-row input:focus, .stage-row textarea:focus, .stage-row select:focus {
+  outline: none; border-color: var(--brass-dim); box-shadow: var(--focus);
 }
 .stage-row textarea { min-height: 44px; resize: vertical; }
 .stage-row-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(110px, 1fr)); gap: 8px; margin-top: 4px; }
 .stages-input > [data-add-stage] { align-self: flex-start; }
 
-/* ------------------------------ egg reward --------------------------------- */
+/* ------------------------------ egg reward ------------------------------- */
 .egg-reward-fields { display: grid; grid-template-columns: 1fr 1fr; gap: 8px; margin-top: 8px; }
-.egg-reward-fields label { display: block; font-size: 11px; color: var(--muted); margin-bottom: 4px; }
+.egg-reward-fields label { display: block; font-size: 10px; color: var(--muted); text-transform: uppercase; letter-spacing: 0.07em; margin-bottom: 4px; }
 .egg-reward-fields select, .egg-reward-fields input {
-  width: 100%; background: var(--panel2); border: 1px solid var(--panel3); color: var(--text);
-  padding: 6px 7px; font: inherit;
+  width: 100%; background: var(--bg); border: 1px solid var(--panel3); border-radius: var(--radius);
+  color: var(--text-bright); padding: 5px 7px; font: inherit; font-size: 12px;
 }
 
-/* --------------------------- result gem (Weapon Enchanting / Armour Infusion recipes) --------------------------- */
+/* ---- result gem (Weapon Enchanting / Armour Infusion recipes) ------------ */
 .result-gem-fields { display: grid; grid-template-columns: 1fr 1fr; gap: 8px; margin-top: 8px; }
-.result-gem-fields label { display: block; font-size: 11px; color: var(--muted); margin-bottom: 4px; }
+.result-gem-fields label { display: block; font-size: 10px; color: var(--muted); text-transform: uppercase; letter-spacing: 0.07em; margin-bottom: 4px; }
 .result-gem-fields select {
-  width: 100%; background: var(--panel2); border: 1px solid var(--panel3); color: var(--text);
-  padding: 6px 7px; font: inherit;
+  width: 100%; background: var(--bg); border: 1px solid var(--panel3); border-radius: var(--radius);
+  color: var(--text-bright); padding: 5px 7px; font: inherit; font-size: 12px;
 }
 
-.errors { background: rgba(163, 58, 58, 0.15); border: 1px solid var(--bad); padding: 10px 12px; margin-bottom: 14px; font-size: 12px; }
-.errors ul { margin: 6px 0 0; padding-left: 18px; }
-
-.editor-actions { display: flex; gap: 8px; justify-content: flex-end; margin-top: 16px; }
-
-.empty { color: var(--muted); font-size: 12px; padding: 20px 0; text-align: center; }
-
-/* --------------------------------- icons ---------------------------------- */
+/* --------------------------------- icons --------------------------------- */
 
 .icon-field { display: flex; align-items: center; gap: 10px; }
 .icon-preview {
-  width: 40px; height: 40px; flex: none;
-  background: var(--panel2); border: 1px solid var(--panel3);
+  width: 42px; height: 42px; flex: none; border-radius: var(--radius);
+  background: var(--bg); border: 1px solid var(--panel3);
   display: flex; align-items: center; justify-content: center;
 }
 .icon-preview img { width: 32px; height: 32px; image-rendering: pixelated; }
-.icon-preview-empty { color: var(--muted); font-size: 16px; }
+.icon-preview-empty { color: var(--dim); font-size: 16px; }
 .icon-field-controls { display: flex; flex-direction: column; gap: 4px; }
 .icon-field-name { font-size: 11px; color: var(--muted); word-break: break-all; }
 .icon-field-controls button { padding: 4px 8px; font-size: 11px; align-self: flex-start; }
 
-.icon-picker { max-width: 720px; max-height: 80vh; overflow-y: auto; }
+.icon-picker { max-width: 760px; max-height: 80vh; overflow-y: auto; }
 .icon-picker-body { margin-bottom: 8px; }
 .icon-picker-section { margin-bottom: 16px; }
 .icon-picker-folder {
-  font-size: 11px; color: var(--muted); text-transform: uppercase;
-  letter-spacing: 0.05em; margin-bottom: 6px;
+  font-size: 10px; color: var(--brass); text-transform: uppercase;
+  letter-spacing: 0.1em; margin-bottom: 6px;
 }
 .icon-picker-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(48px, 1fr)); gap: 6px; }
 .icon-picker-item {
-  width: 48px; height: 48px; padding: 4px;
+  width: 48px; height: 48px; padding: 4px; border-radius: var(--radius);
   background: var(--panel2); border: 1px solid var(--panel3); cursor: pointer;
   display: flex; align-items: center; justify-content: center;
 }
-.icon-picker-item:hover { border-color: var(--muted); }
-.icon-picker-item.selected { border-color: var(--brass); background: var(--panel3); }
+.icon-picker-item:hover { border-color: var(--muted); background: var(--panel3); }
+.icon-picker-item.selected { border-color: var(--brass); background: var(--brass-wash); }
 .icon-picker-item img { width: 32px; height: 32px; image-rendering: pixelated; }
 
 td.icon-cell { width: 1%; padding-right: 0; }
-.table-icon { width: 24px; height: 24px; image-rendering: pixelated; vertical-align: middle; display: block; }
-.table-icon-empty { color: var(--muted); }
+.table-icon { width: 22px; height: 22px; image-rendering: pixelated; vertical-align: middle; display: block; }
+.table-icon-empty { color: var(--dim); }
 
-/* -------------------------------- banners ---------------------------------- */
+/* -------------------------------- banners -------------------------------- */
 
 .table-banner {
   width: 42px; height: 24px; display: block; border-radius: 2px;
@@ -168,13 +369,13 @@ td.icon-cell { width: 1%; padding-right: 0; }
   position: relative;
   width: 100%; max-width: 420px; height: 130px;
   background-color: var(--panel2); background-size: cover;
-  border: 1px solid var(--panel3); border-radius: 4px;
+  border: 1px solid var(--panel3); border-radius: var(--radius);
   cursor: crosshair; touch-action: none; overflow: hidden;
   user-select: none;
 }
 .banner-preview-empty {
   position: absolute; inset: 0; display: flex; align-items: center; justify-content: center;
-  padding: 12px; text-align: center; font-size: 11px; color: var(--muted);
+  padding: 12px; text-align: center; font-size: 11px; color: var(--dim);
 }
 .banner-focus-marker {
   position: absolute; width: 16px; height: 16px; margin: -8px 0 0 -8px;
@@ -194,122 +395,139 @@ td.icon-cell { width: 1%; padding-right: 0; }
 .banner-field-buttons { display: flex; gap: 6px; }
 .banner-field-buttons button { padding: 4px 8px; font-size: 11px; }
 
-.banner-picker { max-width: 820px; }
+.banner-picker { max-width: 840px; }
 .banner-picker-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(140px, 1fr)); gap: 10px; }
 .banner-picker-item {
-  padding: 4px; background: var(--panel2); border: 1px solid var(--panel3); cursor: pointer;
+  padding: 4px; background: var(--panel2); border: 1px solid var(--panel3);
+  border-radius: var(--radius); cursor: pointer;
   display: flex; flex-direction: column; gap: 4px; align-items: stretch;
 }
 .banner-picker-item:hover { border-color: var(--muted); }
-.banner-picker-item.selected { border-color: var(--brass); background: var(--panel3); }
+.banner-picker-item.selected { border-color: var(--brass); background: var(--brass-wash); }
 .banner-picker-thumb {
   width: 100%; height: 78px; background-size: cover; background-position: center;
   background-color: var(--panel3); border-radius: 2px;
 }
 .banner-picker-name { text-align: left; word-break: break-all; }
 
-/* ----------------------------- loot table field ---------------------------- */
+/* ----------------------------- loot table field -------------------------- */
 
 .loot-rows { display: flex; flex-direction: column; gap: 4px; margin-bottom: 8px; }
 .loot-row {
   display: flex; align-items: center; gap: 8px;
   background: var(--panel2); border: 1px solid var(--panel3);
-  padding: 6px 8px;
+  border-radius: var(--radius); padding: 5px 8px;
 }
 .loot-row-name { flex: 1; font-size: 12px; }
-.loot-row-chance { width: 60px; background: var(--panel); border: 1px solid var(--panel3); color: var(--text); padding: 4px; }
+.loot-row-chance {
+  width: 62px; background: var(--bg); border: 1px solid var(--panel3);
+  border-radius: var(--radius); color: var(--text-bright); padding: 4px 6px;
+  font: inherit; font-size: 12px;
+}
 .loot-row .remove { padding: 4px 8px; }
 
 .loot-picker-grid { display: flex; flex-direction: column; gap: 4px; }
 .loot-picker-item {
   display: flex; align-items: center; gap: 8px;
   background: var(--panel2); border: 1px solid var(--panel3);
-  padding: 6px 8px; cursor: pointer; text-align: left;
+  border-radius: var(--radius); padding: 5px 8px; cursor: pointer; text-align: left;
 }
-.loot-picker-item:hover { border-color: var(--brass); }
+.loot-picker-item:hover { border-color: var(--brass); background: var(--panel3); }
 .loot-picker-thumb { width: 24px; height: 24px; image-rendering: pixelated; flex: none; }
 .loot-picker-thumb-empty {
   display: flex; align-items: center; justify-content: center;
-  background: var(--panel); border: 1px solid var(--panel3); color: var(--muted); font-size: 12px;
+  background: var(--bg); border: 1px solid var(--panel3); color: var(--dim); font-size: 12px;
 }
 
 /* -------------------------------- patches -------------------------------- */
 
 .patch-git-status {
-  border: 1px solid var(--panel3);
-  background: var(--panel);
-  padding: 10px 12px;
-  font-size: 11px;
+  border: 1px solid var(--panel3); border-radius: var(--radius);
+  background: var(--panel); padding: 10px 12px; font-size: 11px;
 }
-.patch-git-status.dirty { border-color: var(--brass); }
+.patch-git-status.dirty { border-color: var(--brass-dim); background: var(--brass-wash); }
 .patch-git-status pre { max-height: 140px; overflow-y: auto; font-size: 10px; color: var(--muted); }
 
-.patch-list { display: flex; flex-direction: column; gap: 6px; margin-bottom: 8px; }
+.patch-list { display: flex; flex-direction: column; gap: 4px; margin-bottom: 8px; }
 .patch-item {
   display: flex; align-items: center; gap: 10px;
-  border: 1px solid var(--panel3); background: var(--panel);
-  padding: 8px 10px; cursor: pointer;
+  border: 1px solid var(--panel3); border-radius: var(--radius);
+  background: var(--panel); padding: 7px 10px; cursor: pointer;
+  transition: border-color 90ms, background 90ms;
 }
-.patch-item.selected { border-color: var(--brass); background: var(--panel2); }
+.patch-item:hover { border-color: #45415a; background: var(--panel2); }
+.patch-item.selected { border-color: var(--brass-dim); background: var(--brass-wash); }
 .patch-item input { accent-color: var(--brass); }
-.patch-name { font-size: 12px; font-weight: bold; }
+.patch-name { font-size: 12px; font-weight: 700; color: var(--text-bright); }
 
-.patch-result { margin: 8px 0 4px; border-left: 3px solid var(--panel3); padding: 6px 10px; font-size: 11px; }
+.patch-result {
+  margin: 8px 0 4px; border-left: 3px solid var(--panel3);
+  background: var(--panel); border-radius: 0 var(--radius) var(--radius) 0;
+  padding: 6px 10px; font-size: 11px;
+}
 .patch-result.good { border-left-color: var(--good); }
 .patch-result.bad { border-left-color: var(--bad); }
-.patch-result-label { font-weight: bold; margin-bottom: 4px; }
+.patch-result-label { font-weight: 700; margin-bottom: 4px; color: var(--text-bright); }
 .patch-result pre {
   max-height: 220px; overflow-y: auto; white-space: pre-wrap; word-break: break-word;
-  background: var(--panel); border: 1px solid var(--panel3); padding: 6px 8px; font-size: 10px;
+  background: var(--bg); border: 1px solid var(--panel3); border-radius: var(--radius);
+  padding: 6px 8px; font-size: 10px; color: var(--muted);
 }
 
-/* --------------------------------- shared text utilities ---------------------------------- */
+/* --------------------------- shared text utilities ----------------------- */
 /* Used throughout app.js (loot/banner pickers, tuning view, etc.) but never
    actually defined -- previously rendered as plain unstyled text wherever
    used. Defined properly here rather than left as dead classes. */
 .tiny { font-size: 11px; }
 .muted { color: var(--muted); }
 
-/* --------------------------------- tuning view ---------------------------------- */
+/* ------------------------------- tuning view ----------------------------- */
 
 .tuning-toolbar .tuning-search {
-  flex: 1; max-width: 320px; background: var(--panel2); border: 1px solid var(--panel3);
-  color: var(--text); padding: 7px 8px; font: inherit;
+  flex: 1; max-width: 340px; background: var(--bg); border: 1px solid var(--panel3);
+  border-radius: var(--radius); color: var(--text-bright); padding: 6px 9px; font: inherit; font-size: 12px;
 }
+.tuning-toolbar .tuning-search:focus { outline: none; border-color: var(--brass-dim); box-shadow: var(--focus); }
 .tuning-summary { margin-bottom: 12px; }
 
-.tuning-groups { display: flex; flex-direction: column; gap: 8px; }
-.tuning-section { border: 1px solid var(--panel3); background: var(--panel); }
+.tuning-groups { display: flex; flex-direction: column; gap: 6px; }
+.tuning-section {
+  border: 1px solid var(--panel3); border-radius: var(--radius);
+  background: var(--panel); overflow: hidden;
+}
 .tuning-section-head {
   width: 100%; display: flex; align-items: center; gap: 8px;
-  background: var(--panel2); border: none; color: var(--text);
-  padding: 9px 12px; cursor: pointer; text-align: left;
+  background: var(--panel2); border: none; border-radius: 0; color: var(--text-bright);
+  padding: 8px 12px; cursor: pointer; text-align: left;
 }
 .tuning-section-head:hover { background: var(--panel3); }
 .tuning-section-arrow { width: 10px; color: var(--brass); flex: none; }
-.tuning-section-name { font-weight: bold; }
-.tuning-section-count { flex: none; }
+.tuning-section-name { font-weight: 700; }
+.tuning-section-count { flex: none; color: var(--muted); }
 .tuning-section-changed {
-  flex: none; font-size: 10px; color: var(--brass); border: 1px solid var(--brass);
-  border-radius: 10px; padding: 1px 8px;
+  flex: none; font-size: 10px; color: var(--brass); border: 1px solid var(--brass-dim);
+  background: var(--brass-wash); border-radius: 10px; padding: 1px 8px;
 }
 
 .tuning-section-body { display: flex; flex-direction: column; }
 .tuning-row {
   display: flex; align-items: center; justify-content: space-between; gap: 16px;
-  padding: 9px 12px; border-top: 1px solid var(--panel3); flex-wrap: wrap;
+  padding: 8px 12px; border-top: 1px solid rgba(255, 255, 255, 0.05); flex-wrap: wrap;
 }
-.tuning-row-changed { border-left: 3px solid var(--brass); }
+.tuning-row:hover { background: rgba(255, 255, 255, 0.02); }
+.tuning-row-changed { border-left: 3px solid var(--brass); padding-left: 9px; }
 .tuning-row-main { min-width: 220px; flex: 2; }
-.tuning-row-label { font-size: 12px; }
-.tuning-row-id { font-family: monospace; }
-.tuning-row-desc { margin-top: 2px; max-width: 480px; }
+.tuning-row-label { font-size: 12px; color: var(--text-bright); }
+.tuning-row-id { font-family: inherit; color: var(--muted); }
+.tuning-row-desc { margin-top: 2px; max-width: 480px; color: var(--muted); }
 
 .tuning-row-controls { display: flex; align-items: center; gap: 8px; flex: none; flex-wrap: wrap; }
 .tuning-value-input {
-  width: 90px; background: var(--panel2); border: 1px solid var(--panel3); color: var(--text);
-  padding: 5px 7px; font: inherit;
+  width: 92px; background: var(--bg); border: 1px solid var(--panel3); border-radius: var(--radius);
+  color: var(--text-bright); padding: 5px 7px; font: inherit; font-size: 12px;
+  font-variant-numeric: tabular-nums;
 }
-.tuning-row-changed .tuning-value-input { border-color: var(--brass); }
-.tuning-range, .tuning-default { white-space: nowrap; }
+.tuning-value-input:focus { outline: none; border-color: var(--brass-dim); box-shadow: var(--focus); }
+.tuning-row-changed .tuning-value-input { border-color: var(--brass-dim); background: var(--brass-wash); }
+.tuning-range, .tuning-default { white-space: nowrap; color: var(--dim); }
 .tuning-reset, .tuning-full-edit { padding: 4px 8px; font-size: 11px; }
