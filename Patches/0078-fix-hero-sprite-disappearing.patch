diff --git a/src/styles/app.css b/src/styles/app.css
index 829040b..11b32e9 100644
--- a/src/styles/app.css
+++ b/src/styles/app.css
@@ -927,16 +927,22 @@ input[type='range'] {
    * z-index below, actually fixes both symptoms this time.
    */
   position: relative;
-  height: 130px;
   width: 220px;
   overflow: visible;
-  /* Reserves the vertical space the quest-mark used to get "for free" by
-     sitting in normal flow above this row -- removing it from flow (see
-     .quest-mark below) also removed that reserved space, which is why the
-     sprite shifted up into where the icon used to have clearance. This
-     restores the original spacing explicitly instead of leaving it to
-     flow that no longer exists. */
-  margin-top: 40px;
+  /*
+   * Reduced from an earlier attempt that added a combined +50px (130px
+   * explicit height, up from a natural ~120px, plus a 40px margin) in a
+   * window that's only 300px tall total. .idle-root anchors content to
+   * the BOTTOM (justify-content: flex-end), so excess total height pushes
+   * earlier elements -- this one specifically -- up and off the TOP of
+   * the window instead of growing the window or scrolling. Confirmed as
+   * the actual cause of the hero sprite disappearing entirely, not a
+   * hypothetical. No explicit height anymore (sizes naturally to the
+   * knight-button again); margin-top cut to a modest 16px rather than
+   * removed outright -- some clearance for the quest-mark above is still
+   * worth keeping, just not enough to risk overflowing the window again.
+   */
+  margin-top: 16px;
 }
 
 .carousel-arrow {
