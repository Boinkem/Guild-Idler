diff --git a/src/game/engine.ts b/src/game/engine.ts
index aad690c..629ac10 100644
--- a/src/game/engine.ts
+++ b/src/game/engine.ts
@@ -804,6 +804,14 @@ export class GameEngine {
     void this.saveNow();
   }
 
+  buyRaidUpgrade(id: string) {
+    const error = GuildManager.buyRaidUpgrade(this.state, id);
+    if (error) return this.say(error);
+    playSound('purchase');
+    this.notify();
+    void this.saveNow();
+  }
+
   levelUpVendor(vendorId: Parameters<typeof GuildManager.levelUpVendor>[1]) {
     const error = GuildManager.levelUpVendor(this.state, vendorId);
     if (error) return this.say(error);
diff --git a/src/game/managers/SaveManager.ts b/src/game/managers/SaveManager.ts
index 7614aa0..b76ea24 100644
--- a/src/game/managers/SaveManager.ts
+++ b/src/game/managers/SaveManager.ts
@@ -96,6 +96,7 @@ export function createInitialState(now = Date.now()): GameState {
     completedRaidDifficulties: [],
     notifications: [],
     seenGuidance: [],
+    raidUpgrades: {},
   };
 }
 
@@ -276,6 +277,11 @@ const MIGRATIONS: Record<number, Migration> = {
     notifications: (save.notifications as unknown[] | undefined) ?? [],
     seenGuidance: (save.seenGuidance as string[] | undefined) ?? [],
   }),
+  17: (save) => ({
+    ...save,
+    version: 18,
+    raidUpgrades: (save.raidUpgrades as Record<string, number> | undefined) ?? {},
+  }),
 };
 
 export const SaveManager = {
