diff --git a/guild-idler-status.md b/guild-idler-status.md
index be546cb..2d58071 100644
--- a/guild-idler-status.md
+++ b/guild-idler-status.md
@@ -28058,3 +28058,71 @@ durability bar aligned with the divider line almost exactly matching the
 border with room to spare. Vendor shop cards (a structurally different
 card shape that never used `.item-card-meta-row`) re-checked for
 regressions and confirmed unaffected.
+
+### Rarity card follow-up #3: the actual root cause, plus the last visible box (patch 0342)
+
+```discord-update
+Dev Update | Rarity Cards
+- Fixed rarity card text landing in the wrong spot for real (found the actual cause this time)
+- Empty gear slots no longer show any box at all -- just the outline art over the scene
+- Removed a leftover outline still visible around Vendor stock cards
+```
+
+Direct follow-up report with a fresh screenshot, on top of 0340/0341's own
+fixes for this same card redesign. This round found the actual root
+cause behind why positioning kept only half-working across patches,
+rather than nudging numbers again.
+
+**The real bug, finally found.** Every `padding-left`/`padding-top` fix
+in 0340 and 0341 was silently losing to a much older rule from patch
+0322 -- `.item-card.rarity-card .item-card-summary { padding: 12px
+14px; }` -- which has three classes of specificity against this
+feature's two, and was never overridden because only `align-items` had
+been marked `!important`, not the padding properties sitting right next
+to it. This explains everything reported across every round: Vendor
+cards use `.rarity-banner-content`, a class the old rule never targets,
+which is exactly why they were the one card type that already looked
+right -- not because they got special treatment, but because they were
+the only ones NOT fighting this old rule in the first place. Every
+`padding-left`/`padding-top` in this feature's CSS is `!important` now.
+With the name actually landing where the padding says it should,
+rarity/durability (and Vendor's level/price, `.item-card-meta-row`)
+moved from `top: 42%` to `top: 56%` -- clearly below the frame's dotted
+divider line instead of straddling it, per a direct annotated report.
+Empty slots' "Empty" label gets the same treatment now too, for
+consistency between a slot's filled and empty states.
+
+**The "purple box," finally gone for good.** Two separate causes, found
+one at a time as each got fixed and the next became visible underneath:
+1. `.item-card.empty { opacity: 0.6 }` was dimming the WHOLE element,
+   including its own solid background rectangle -- confirmed live by
+   forcing opacity:1 and watching a translucent haze vanish entirely.
+   Fixed last round by restoring full opacity for the Silver-framed
+   empty slots specifically.
+2. Even at full opacity, a real rectangular card (border + solid
+   background) was still visible as a box. Direct follow-up: remove it
+   entirely rather than just fix its opacity -- the Silver outline art
+   is a complete, closed shape on its own (a full ring + frame border),
+   so there's nothing left for a separate card background to add. Empty
+   gear slots now render with no card background or border at all, just
+   the line art directly over the scene.
+
+**The last visible outline, on Vendor cards specifically.** Direct
+follow-up with a zoomed screenshot: even after border: none, Vendor
+cards (EquipmentShopCard/ConsumableShopCard) still showed a thin gold
+outline. Vendor cards are the only `.card`-based usage of this feature
+(everything else is `.item-card`-based) -- traced to `.card`'s own
+`box-shadow: var(--bevel), var(--carved-line)`, and `--carved-line`
+specifically is `0 0 0 1px color-mix(...)`, a solid-color outline drawn
+via box-shadow rather than the `border` property, so `border: none`
+never touched it. `.item-card` never had this problem since its own
+edge is a real `border`, already cleared. `box-shadow: none` added.
+
+**Verified.** `npx tsc --noEmit` and `npx vite build` both pass clean.
+Every fix in this entry was re-screenshotted against the exact live
+views the corresponding report was made against -- StashCard/SlotCard
+name and meta-row position, every empty gear slot showing no box at all
+against the scene art, and a tight zoom on a Vendor stock card showing a
+clean edge with no outline. Also re-checked Inventory after the Vendor-
+specific box-shadow fix to confirm the shared `.rarity-frame-card` class
+didn't regress anything there -- it didn't.
diff --git a/src/styles/app.css b/src/styles/app.css
index 5112ace..fd3cad0 100644
--- a/src/styles/app.css
+++ b/src/styles/app.css
@@ -151,6 +151,41 @@
   overflow: hidden;
 }
 .item-card.empty { opacity: 0.6; }
+/*
+ * Direct report with a live before/after comparison: this blanket
+ * opacity applies to the WHOLE element, including its own solid dark
+ * background rectangle (.item-card's own color-mix panel fill) -- not
+ * just the decorative content on top of it. Against the busy, colorful
+ * tab-scene art these cards sit over, a 60%-opacity solid rectangle
+ * reads as a translucent "haze" rather than a subtly-dimmed card,
+ * confirmed by forcing opacity:1 on a live empty slot and comparing --
+ * the haze vanished entirely, the card just looked like a normal solid
+ * card again. Restored to full opacity specifically for the new Silver-
+ * framed empty gear slots (rarity-frame-card-outline, patch 0338) --
+ * that frame's own muted grey/silver outline already reads as visually
+ * distinct from a real item's colorful rarity frame, so the emptiness
+ * signal doesn't depend on this opacity dimming anymore the way the
+ * plain pre-0338 placeholder did. Scoped narrowly rather than changing
+ * the base rule, since .item-card.empty.consumable-empty (a different,
+ * non-framed empty state) still wants its own dimming behavior. */
+.item-card.empty.rarity-frame-card-outline { opacity: 1; }
+/*
+ * Direct follow-up report: even at full opacity, the card still showed
+ * as a visible rectangular "box" -- .item-card's own base border (1px
+ * solid) and solid background-color (color-mix panel fill) are still a
+ * real, flat-edged rectangle, just no longer translucent. Asked to
+ * remove it entirely rather than just fix its opacity: the Silver
+ * outline art is the ONLY visual this slot needs (it's a closed ring +
+ * frame line-art already, a complete-looking shape on its own), so
+ * there's nothing left for a separate rectangular card background to
+ * usefully add here -- unlike a painted rarity frame, which fills its
+ * own frame edge-to-edge and doesn't need one either (.rarity-frame-card
+ * already sets background-color: transparent for the same reason).
+ */
+.item-card.empty.rarity-frame-card-outline {
+  background-color: transparent;
+  border: none;
+}
 .item-card-summary {
   display: flex;
   align-items: center;
@@ -5405,6 +5440,18 @@ button.harvest-item:hover {
   aspect-ratio: 1536 / 1024;
   border: none;
   background-color: transparent;
+  /*
+   * Patch 0342, direct follow-up report: Vendor cards (the only .card-
+   * based usage of this class -- EquipmentShopCard/ConsumableShopCard,
+   * everything else is .item-card-based) still showed a thin outline
+   * even after border: none above. Traced to .card's own box-shadow
+   * (var(--bevel), var(--carved-line)) -- --carved-line specifically is
+   * `0 0 0 1px color-mix(...)`, a solid-color 1px outline drawn via
+   * box-shadow rather than the border property, so border: none alone
+   * never touched it. .item-card never had this problem since it uses a
+   * real `border` (not box-shadow) for its own edge, already cleared.
+   */
+  box-shadow: none;
   /*
    * Patch 0340, direct report with screenshots, confirmed live: without
    * this, the browser smoothly (bilinearly) downscales the frame art
@@ -5423,8 +5470,8 @@ button.harvest-item:hover {
 }
 .rarity-frame-card .item-card-summary,
 .rarity-frame-card .rarity-banner-content {
-  padding-left: 34%;
-  padding-top: 21%;
+  padding-left: 34% !important;
+  padding-top: 21% !important;
   min-height: 100%;
   box-sizing: border-box;
   /* Patch 0340, direct report: the name was vertically centered in the
@@ -5440,33 +5487,31 @@ button.harvest-item:hover {
      enough -- measured directly off the source art, the frame's own top
      double-line border runs from ~14% to ~20% down, so a name pinned at
      12% sat ON TOP of that border instead of below it. 21% clears it
-     with a little room to spare. */
+     with a little room to spare.
+     Patch 0342, direct follow-up report: found the ACTUAL reason the
+     name still wasn't landing where 0341 intended, on real item cards
+     specifically (StashCard/SlotCard/etc, NOT the empty-slot outline
+     variant) -- a much older rule, `.item-card.rarity-card .item-card-
+     summary { padding: 12px 14px; }` (patch 0322), has three classes of
+     specificity against this rule's two, and was silently winning for
+     padding-left/padding-top this entire time since neither was marked
+     !important before now (only align-items was). Vendor cards never
+     hit this because they use `.rarity-banner-content`, a different
+     class the old rule never targeted -- confirmed as the reason Vendor
+     cards' name position already read correctly while every card using
+     `.item-card-summary` didn't, exactly matching what was reported. */
   align-items: flex-start !important;
 }
-/* Patch 0341, direct follow-up report with a before/after mockup: pills
-   and other secondary content (durability bar, count, etc) were still
-   landing immediately under the name, cramped together at the top --
-   the request was to spread them out, landing secondary content down
-   near the icon's own row (the dotted divider line, ~49% down) instead.
-   Absolute positioning here, not a stretch+space-between flex approach
-   (tried first, reverted) -- that approach relies on percentage heights
-   resolving correctly through a chain of nested flex/aspect-ratio
-   containers, which turned out to be unreliable in practice: caught live
-   with a real screenshot showing the durability bar pushed straight out
-   past the bottom of the visible frame entirely, not just misplaced
-   within it. Same proven technique the icon positioning below already
-   uses instead -- percentages measured directly against the card,
-   independent of any ancestor's own height resolution. `top: 42%`
-   deliberately sits a little above the dotted line's own ~49% so a
-   compact one-line row of pills reads as roughly centered ON the line,
-   not starting at it and hanging below. Left/right match the body's own
-   horizontal bounds (34% padding-left, ~6% right margin used elsewhere
-   on this card). */
+/* Patch 0342, direct follow-up report with an annotated screenshot:
+   rarity/durability (and Vendor's level/price) needs to sit clearly
+   BELOW the frame's decorative dotted line, not centered on top of it --
+   the previous `top: 42%` straddled the line itself. 56% sits solidly
+   underneath it instead. */
 .rarity-frame-card .item-card-meta-row {
   position: absolute;
   left: 34%;
   right: 6%;
-  top: 42%;
+  top: 56%;
 }
 /* Outline-only variant (Silver, empty slots) -- deliberately does NOT
    set background-color: transparent like .rarity-frame-card above. The
@@ -5484,12 +5529,25 @@ button.harvest-item:hover {
   image-rendering: pixelated;
 }
 .rarity-frame-card-outline .item-card-summary {
-  padding-left: 34%;
-  padding-top: 21%;
+  padding-left: 41% !important;
+  padding-top: 21% !important;
   min-height: 100%;
   box-sizing: border-box;
   align-items: flex-start !important;
 }
+/* Patch 0342, direct follow-up report: "Empty" needs to sit below the
+   dotted line too, matching where rarity/durability lands on a real
+   item's card -- for visual consistency between the filled and empty
+   states of the exact same slot. Empty slots don't use
+   .item-card-meta-row (that class carries actual pills on a real item;
+   "Empty" is just a plain second line, see SlotCard's empty branch in
+   EquipmentPanel.tsx), so this targets that second line directly via
+   its own existing class instead of adding a new wrapper. */
+.rarity-frame-card-outline .item-card-body > .tiny.muted {
+  position: absolute;
+  left: 41%;
+  top: 56%;
+}
 /* The icon sits inside the frame's own circular socket -- position and
    size measured directly off the source art / the approved mockup,
    including the later "increase icon size" follow-up (fill bumped from
diff --git a/src/ui/panels/VendorsPanel.tsx b/src/ui/panels/VendorsPanel.tsx
index 5c09d14..5258414 100644
--- a/src/ui/panels/VendorsPanel.tsx
+++ b/src/ui/panels/VendorsPanel.tsx
@@ -1088,8 +1088,8 @@ function EquipmentShopCard({
         <div className="rarity-banner-content row" style={{ gap: 10, alignItems: 'center' }}>
           <ItemIcon slot={def.slot} icon={def.icon} size={41} />
           <div style={{ minWidth: 0, flex: 1 }}>
-            <div style={{ color: RARITY_COLOR[def.rarity], fontWeight: 700, fontSize: 13 }}>{def.name}</div>
-            <div className="tiny muted">Lv {displayLevel} · {formatGold(price)}</div>
+            <div className="item-card-name" style={{ color: RARITY_COLOR[def.rarity], fontWeight: 700, fontSize: 13 }}>{def.name}</div>
+            <div className="item-card-meta-row tiny muted">Lv {displayLevel} · {formatGold(price)}</div>
           </div>
         </div>
       </div>
@@ -1159,8 +1159,8 @@ function ConsumableShopCard({
         <div className="rarity-banner-content row" style={{ gap: 10, alignItems: 'center' }}>
           <ConsumableIcon icon={def.icon} glyph={def.glyph} size={41} />
           <div style={{ minWidth: 0, flex: 1 }}>
-            <div style={{ color: RARITY_COLOR[def.rarity], fontWeight: 700, fontSize: 13 }}>{def.name}</div>
-            <div className="tiny muted">{formatGold(price)}</div>
+            <div className="item-card-name" style={{ color: RARITY_COLOR[def.rarity], fontWeight: 700, fontSize: 13 }}>{def.name}</div>
+            <div className="item-card-meta-row tiny muted">{formatGold(price)}</div>
           </div>
         </div>
       </div>
